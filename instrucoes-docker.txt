Rodar os comandos na ordem:

docker-compose up -d --build

docker-compose run app


Deletar os containers e volumes:
docker rm -vf $(docker ps -aq) && docker volume prune -f

Remover apenas volume do mysql:
docker rm -v mysql_container